var myWindowId;

// Need to retrieve inbox id from configuration
const contentBox = document.querySelector("#context");

// Load pre defined templates
console.log("test");

// According to users choice, query for template


// Setup content box
contentBox.textContent = 'abc';
